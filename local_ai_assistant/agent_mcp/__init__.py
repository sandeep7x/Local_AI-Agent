# agent_mcp/__init__.py
# MCP (Model Context Protocol) integration layer.
# This package wraps existing agents as MCP tools without modifying them.
# Named 'agent_mcp' to avoid shadowing the installed 'mcp' SDK package.
